# a-stock-data 开工前 60 秒自检卡片

每次开始非琐碎切片前，先用 60 秒完成本卡。

如果 60 秒后仍然不能明确目标、范围、避免项和验证方式，不要开始编码，先补齐假设、缩小切片或明确验证方式。

---

## 0-15 秒：我现在到底在做什么

- 当前属于哪个 Phase / 哪张卡？
- 当前只要交付哪一个最小结果？
- 这一步是做骨架、Provider、Service、Route，还是测试？

通过标准：

- 能用一句话说清当前切片目标

错误信号：

- “我先把这块都搭起来再说”
- “我顺手把后面几步一起做了”

## 15-30 秒：我是不是在偷偷加复杂度

- 当前真的需要新抽象吗？
- 当前有第二个真实调用点吗？
- 这一步新增的复杂度，能直接帮助当前接口通过测试吗？

通过标准：

- 当前实现是解决今天的问题，不是提前解决未来的问题

错误信号：

- 想加 BaseProvider / Factory / Repository / Registry
- 想提前加 Redis / Celery / 多实例缓存 / 动态配置系统
- 想把单个接口先设计成平台

## 30-45 秒：我是不是改多了

- 当前要改的文件是否只围绕这一条主线？
- 有没有顺手重构、顺手改风格、顺手整理目录的冲动？
- 有没有改到与当前失败测试无关的地方？

通过标准：

- 每一处改动都能直接追溯到当前切片或当前失败测试

错误信号：

- “顺手把这个也统一一下”
- “顺手把已有同步请求改成 async”
- “顺手把别的模块也按新风格改掉”

## 45-60 秒：我怎么证明这一步完成了

- 当前这一步的最小验证命令是什么？
- 是单测、契约测试、API 集成测试，还是更窄的可执行检查？
- 如果失败，我是否知道先修哪一层？

通过标准：

- 能明确写出一个可执行验证动作

错误信号：

- “先写完再统一跑 pytest”
- “应该差不多能用”
- “先不测，后面再看”

---

## 开工模板

如果要写成 4 行，直接用这个模板：

```text
Assumption: 这一步只做 ________
Scope: 只改 ________
Avoid: 不引入 ________
Verify: 跑 ________
```

示例：

```text
Assumption: 这一步只做 quote API
Scope: 只改 tencent provider、market service、market route、quote tests
Avoid: 不引入 BaseProvider 和缓存层
Verify: pytest tests/contract/test_tencent_provider.py ; pytest tests/integration/test_quote_api.py
```

## 一句话判断

如果当前方案不能同时满足下面 4 条，就不要开工：

- 目标够小
- 改动够窄
- 实现够简单
- 验证够明确

满足这 4 条，再开始写代码。