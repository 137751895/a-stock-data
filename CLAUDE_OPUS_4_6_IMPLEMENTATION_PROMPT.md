# Claude Opus 4.6 执行提示词：将 a-stock-data 完整改造为 API 服务

你将把当前仓库中的 a-stock-data 从 Skill 形态完整改造为可运行、可测试、可验证、可维护的 API 服务。

交付标准不是“看起来做完了”，而是“经过验证地做完了”。

在整个任务中，你必须表现出以下能力：

- 能从现有仓库文档中抽取真实实现边界，而不是凭空设计
- 能把复杂的上游数据源差异收敛到清晰的模块边界中
- 能持续实施最小切片开发，而不是大而散的重写
- 能主动运行测试、定位错误、修复问题、再次验证，直到结果稳定
- 能在全部实现完成后，回到最初设计，逐条核对实际实现是否符合设计目标

你必须从头到尾坚持工程质量，不得为了“尽快结束”而跳过测试、回避错误、忽略回归风险或省略最终审计。

## 一、任务目标

把当前仓库中的 a-stock-data 从“Claude Skill 文档 + 内嵌 Python 代码”改造成“标准化的 Python API 服务”。

最终目标包括：

- 从 SKILL.md 中提炼并抽取可维护的 Python 模块
- 建立服务端应用骨架，推荐使用 FastAPI
- 将核心能力暴露为版本化 HTTP API
- 保留并验证当前项目的业务语义，不随意改写
- 建立自动化测试体系
- 在开发过程中持续执行“实现 -> 测试 -> 修复 -> 再验证”的循环
- 在全部开发完成后，输出一份“初始设计 vs 最终实现”的差异审计

## 二、你必须先理解的项目事实

在开始编码前，你必须先阅读并理解当前仓库中的至少这些文件：

- README.md
- SKILL.md
- CHANGELOG.md

你必须从这些文件中提炼出以下事实，并以此指导实现：

1. 当前仓库的核心逻辑主要写在 SKILL.md 中，而不是独立 .py 模块中。
2. 项目目前覆盖 7 层能力、28 个端点、13 个数据源。
3. 大多数数据源是 HTTP 直连，只有 mootdx 走 TCP。
4. 一部分实现返回 dict/list，一部分返回 pandas DataFrame。
5. 一部分能力依赖特殊 Header、特殊参数或环境变量，例如：
   - iwencai 的 API Key 和 X-Claw 头
   - 东财全球资讯的 req_trace
   - 巨潮公告的 orgId 规则
6. CHANGELOG 中记录的是已知高风险回归点，必须纳入测试，不得忽略。

你不能假设仓库里已经有现成的 API 服务层。如果没有，你就要建立它。

## 三、不可违背的设计原则

1. 不要在运行时解析 Markdown 执行代码。你必须把 SKILL.md 中的实现抽取成正常 Python 模块。
2. 保持当前业务语义等价。不要为了“结构更优雅”而随意改写数据口径、字段意义或上游选择。
3. 不要一开始就试图把全部 28 个接口一次性重写完成。要按阶段、按切片推进。
4. 路由层只负责 HTTP 输入输出，不负责抓数和复杂业务逻辑。
5. 上游差异必须封装到 Provider / Client 层，不要扩散到 routes。
6. 纯计算逻辑必须收敛到独立的 domain / utils 层，并且可单元测试。
7. 每次完成一个实质性切片后，必须立即运行该切片的最小验证，不得继续盲目堆代码。
8. 如果测试失败，必须优先修复当前切片，先验证再扩展，不得把失败留到最后统一处理。
9. 在你宣布任务完成前，必须执行最终的全量验证与设计对比审计。

此外，你必须显式启用 [andrej-karpathy-skills-main/skills/karpathy-guidelines/SKILL.md](andrej-karpathy-skills-main/skills/karpathy-guidelines/SKILL.md) 中的四条行为规则，并将其转化为当前任务的执行纪律：

同时，你必须把 [API_SERVICE_PROJECT_EXAMPLES.md](API_SERVICE_PROJECT_EXAMPLES.md) 作为当前项目的项目化样例库来使用。它不是可选参考，而是你判断“当前方案是不是过度设计、是不是偏离任务边界、是不是缺少验证”的直接对照物。

另外，在开始每个非琐碎切片前，你必须先过一遍 [API_SERVICE_60S_PRESTART_CHECKLIST.md](API_SERVICE_60S_PRESTART_CHECKLIST.md)。如果 60 秒后仍然不能明确目标、范围、避免项和验证方式，就先不要编码。

### 1. 编码前思考

- 在每个非琐碎实现切片开始前，先输出：假设、歧义、最小可行切片、验证方式。
- 如果存在多个合理解释，不要静默选择。先说明分歧，再决定是否需要一个最小澄清问题。
- 只有当歧义会改变 API 契约、目录结构、数据源选择、鉴权方式、缓存策略时，才允许停下来提问。
- 如果歧义不阻塞主路径，请明确写出你的假设并继续推进。

### 2. 简洁优先

- 不要为单个调用方提前设计 repository、factory、base client、多态抽象或配置层。
- 不要添加用户没有要求的“灵活性”“可配置性”“未来扩展能力”。
- 不要为不真实的边界情况写大量防御性代码。
- 如果一个切片写到了明显超过问题复杂度的代码量，先停下来简化，再继续。

### 3. 精准修改

- 每一行改动都必须能直接追溯到当前任务、当前阶段目标或当前失败测试。
- 不要顺手改进相邻代码、注释、格式或做无关重构。
- 只删除因当前改动而产生的无用代码；不要删除原本就存在的无关死代码，除非任务明确要求。

### 4. 目标驱动执行

- 你必须把每个阶段都写成“步骤 -> 验证”的形式。
- 不接受“先全部实现，再统一跑测试”的工作方式。
- 如果某项工作无法写出明确验证方式，先补足成功标准，再开始实现。

## 四、技术栈要求

如果仓库中没有相反约束，默认采用以下实现：

- Python 3.11+
- FastAPI 作为服务框架
- Pydantic 作为请求 / 响应模型
- pydantic-settings 作为配置管理（推荐）
- requests 作为上游 HTTP 客户端
- pandas + lxml 用于同花顺 HTML 表格解析
- pytest 作为测试框架
- FastAPI TestClient 作为 API 集成测试工具
- responses 或 requests-mock 用于 requests 级别的上游 HTTP mock

如需新增工程化工具，可审慎加入：

- ruff
- mypy
- uvicorn

但不要引入与当前任务无关、会显著放大复杂度的依赖。

## 五、推荐的目标结构

你应优先把项目整理为类似这样的结构：

```text
app/
  main.py
  api/routes/
  core/
  schemas/
  providers/
  services/
  domain/
  storage/
tests/
  unit/
  contract/
  integration/
  smoke/
```

你不需要机械地照搬这个结构，但必须保证职责边界清晰：

- routes
- services
- providers
- domain calculations
- config / error / logging
- tests

## 六、你必须按这个顺序推进

### Phase 0：分析与设计

在任何实质代码实现前，你必须先完成这些动作：

1. 阅读 README.md、SKILL.md、CHANGELOG.md、API_SERVICE_PROJECT_EXAMPLES.md。
2. 列出当前所有核心函数及其所属层级。
3. 标注哪些是单源函数，哪些是聚合函数。
4. 识别已知高风险点和回归点。
5. 输出一份设计文档，至少包含：
   - 目标架构
   - 模块划分
   - 首批接口清单
   - 测试策略
   - 缓存策略
   - 错误处理策略

在输出设计文档前，你还必须先输出一个极简前置块：

```text
Assumptions:
- ...

Ambiguities:
- ...

Simplest viable slice:
- ...

Verify:
- ...
```

并且在开始每个 Phase 前，你都要先自查一次：

- 当前方案是否落入 [API_SERVICE_PROJECT_EXAMPLES.md](API_SERVICE_PROJECT_EXAMPLES.md) 中的某个反模式
- 当前切片是否已经小到可以独立验证
- 当前验证方式是否具体到命令、测试或可执行检查

设计完成后，不要停下来等用户确认；如果没有新的阻塞，继续进入实现阶段。

### Phase 1：搭建服务骨架

你必须先实现服务骨架，而不是先写一堆 Provider：

1. 建立应用入口
2. 建立 `pyproject.toml` 或等价依赖清单
3. 建立配置模块
4. 建立统一错误模型
5. 建立统一响应 envelope
6. 建立 health 路由
7. 建立测试基础设施

完成后立刻运行：

- 最小单元测试
- 最小 API 集成测试

如果失败，立即修复，不得继续扩张。

### Phase 2：先实现高价值 MVP 接口

优先实现这些：

- quote
- valuation
- reports
- stock-info
- fund-flow-minute
- margin
- announcements
- news

实现策略：

1. 先抽取公共 helper 和 Provider
2. 再做对应 service
3. 最后接入 route 和 schema
4. 每完成一个接口，马上补对应测试并验证

### Phase 3：扩展信号与资金面

继续实现：

- hot stocks
- northbound realtime / history
- concept blocks
- dragon tiger
- daily dragon tiger
- lockup
- industry ranking
- holders
- dividends
- fund-flow-120d

### Phase 4：扩展新闻、基础数据和 TCP 能力

继续实现：

- global news
- telegraph
- financial reports
- F10
- mootdx quotes / bars / transactions

如果 mootdx 受环境影响不稳定，你可以：

- 先把接口结构实现出来
- 对网络依赖做清晰隔离
- 用 mock 测试覆盖主要逻辑
- 明确记录 live 环境限制

### Phase 5：聚合工作流和最终收尾

实现：

- full valuation workflow
- new target investigation workflow
- batch valuation
- 文档收尾
- 回归测试补齐
- 设计与实现审计

## 七、模块协调要求

你必须让各模块以清晰、稳定、可维护的方式协作：

### 1. 路由层

- 只做参数解析、调用 service、返回 schema
- 不直接 requests
- 不直接做复杂转换

### 2. Provider 层

- 每个 Provider 聚焦一个上游来源
- 封装 URL、headers、分页、编码、鉴权
- 把原始响应转换为内部可用结构

### 3. Service 层

- 负责协调多个 Provider
- 负责聚合、降级、缓存、告警、warnings
- 负责业务流程完整性

### 4. Domain 层

- 只做纯函数计算
- 不依赖网络
- 必须可以稳定单元测试

## 八、你必须特别注意的实现陷阱

以下问题必须显式处理，不能忽略：

1. 腾讯行情字段索引必须经过回归测试：43 是振幅，46 才是 PB。
2. 同花顺一致预期返回 DataFrame，API 层不能直接暴露 DataFrame。
3. iwencai 必须通过环境变量读取 Key，不能硬编码。
4. 东财全球资讯必须带 req_trace，否则会 403。
5. 巨潮公告 stock 参数必须是 code,orgId 格式。
6. 北向历史缓存路径必须可配置，不要写死到用户目录。
7. 百度旧资金流接口已下线，资金流必须走东财 push2。
8. 聚合接口必须支持部分失败，不要因为单一 Provider 失败导致整个接口完全不可用。

## 九、自动测试、自动修复、自动验证循环

这是整个任务最重要的执行纪律。你必须严格执行，直到任务真正完成。

### 9.1 单切片循环

每完成一个最小切片，必须执行：

1. 实现该切片的最小代码
2. 立刻运行与该切片最相关的最小测试
3. 如果失败，先阅读报错并判断根因
4. 只修当前切片，不要在失败状态下继续扩写别的功能
5. 修复后重跑相同测试
6. 通过后再继续下一个切片

你不允许在第一处失败后跳过验证，也不允许把多个未验证切片堆积到一起。

### 9.2 阶段循环

每完成一个阶段，必须执行：

1. 跑该阶段全部单元测试
2. 跑该阶段全部契约测试
3. 跑该阶段 API 集成测试
4. 修复失败项
5. 重新运行同一组测试
6. 通过后再进入下一个阶段

### 9.3 全量循环

在你认为全部完成时，必须执行：

1. 全量单元测试
2. 全量契约测试
3. 全量集成测试
4. lint / type check（若项目已配置）
5. 可选 smoke tests（若环境允许）

只要还有失败，任务就没有完成。继续修复，直到通过，或者明确证明受外部环境阻塞且已给出替代验证。

## 十、测试要求

### 单元测试必须覆盖

- 股票代码归一化
- 市场前缀和 secid 生成
- 巨潮 orgId 生成
- 估值计算：forward PE、PEG、PE digestion
- 腾讯字段解析
- DataFrame 到 JSON 的稳定转换
- 时间格式和 NaN / inf 处理

### 契约测试必须覆盖

- tencent provider
- eastmoney datacenter helper
- eastmoney push2
- ths eps forecast
- iwencai provider
- cninfo provider
- cls provider
- sina provider

### 集成测试必须覆盖

- health
- quote
- valuation
- reports
- stock-info
- fund-flow-minute
- margin
- announcements
- news

### 回归测试必须覆盖

回归测试至少包含 CHANGELOG 中这些历史坑：

- 资金流数据源切换
- 大宗交易报表名变更
- 机构席位筛选逻辑
- req_trace 参数
- cninfo orgId 参数规则

## 十一、你必须优先做“根因修复”，不要做表面修补

如果测试失败，你必须先判断：

- 是数据解析问题
- 是 provider 参数问题
- 是 schema 输出问题
- 是缓存路径问题
- 是 domain 计算问题
- 是测试假设错了

你必须修根因，而不是用临时 if 或吞异常掩盖问题。

你不得通过这些方式伪造成功：

- 粗暴 except Exception 然后返回空结果
- 删除失败测试而不是修复代码
- 注释掉关键验证步骤
- 把 live 问题伪装成逻辑已完成

## 十二、你必须持续维护“任务事实表”

在实现过程中，你必须持续维护一份简洁事实表，至少包括：

- 已完成模块
- 未完成模块
- 当前失败测试
- 已知阻塞
- 下一步最小切片

事实表必须随着开发推进而更新，不能只在开始时写一次。

## 十三、最终必须输出的交付物

在任务结束时，除了代码本身，你必须确保仓库中有这些交付物：

1. API 服务代码
2. 测试代码
3. 运行说明
4. 配置说明
5. 初始设计文档
6. 最终实现审计文档
7. 依赖清单（`pyproject.toml` 或等价文件）

其中“最终实现审计文档”必须明确回答：

- 最初设计了什么
- 最终实现了什么
- 哪些一致
- 哪些偏离
- 偏离的原因是什么
- 偏离是否影响任务目标

## 十四、最终审计要求

在宣布完成前，你必须逐项核对最初设计与最终实现。至少输出如下表格：

| 设计项 | 设计方案 | 实际实现 | 是否一致 | 偏差原因 | 是否影响验收 |
|---|---|---|---|---|---|

你必须对每一个关键模块做出结论，而不是只做笼统总结。

## 十五、执行风格要求

你必须像一个真正负责交付结果的高级工程师那样工作：

- 简洁汇报进度，但不要只汇报不行动
- 面对失败先收敛局部，再扩大范围
- 始终优先最小可验证切片
- 不做无意义的大范围重构
- 不遗漏用户的关键要求
- 不在未验证的情况下宣称完成

你要追求的不是“写得很多”，而是“每一步都经得起验证”。

## 十六、启动指令

现在开始执行任务，并严格遵守以下次序：

1. 读取 README.md、SKILL.md、CHANGELOG.md。
2. 提炼真实函数清单、分层、数据源、风险点。
3. 先输出设计文档并落盘到仓库。
4. 建立 API 服务骨架。
5. 以最小切片逐步实现高价值接口。
6. 每个切片后立即测试，失败立即修复并复测。
7. 完成全部功能后，运行全量验证。
8. 回头对比最初设计与最终实现，输出审计文档。
9. 只有在测试和审计都完成后，才允许宣布任务完成。

不要停留在方案讨论，不要只输出建议，不要提前结束。持续行动，直到任务被可验证地完整交付。

---

如果你在 Claude Code 或类似具备文件编辑、终端、测试运行能力的环境中执行，请默认用户希望你直接在仓库里落地实现，而不是仅给出纸面方案。